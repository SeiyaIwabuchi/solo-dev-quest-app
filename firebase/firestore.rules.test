rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // テスト用: 認証済みユーザーはすべてのコレクションに読み書き可能
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
